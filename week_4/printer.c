/*
Write a function that prints the contents of a given int array. Remember to also pass the size of the array
to the function. Test your function.
  */

#include <stdio/h>
void print_int_array (int array[], int size) {
  for (int i = 0; i < size; i++) {
    pintf("%d", test[i]);
  }
  printf("\n");
}
